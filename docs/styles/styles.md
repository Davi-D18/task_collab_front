# Estilos

Esta seção documenta a estrutura de estilos da aplicação.

## Módulos SCSS

A aplicação utiliza SCSS Modules para estilos com escopo local para cada componente.

## Estilos Globais

Definidos em `global.scss`, incluem variáveis, reset CSS e estilos base.
